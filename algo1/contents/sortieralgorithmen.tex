\section*{Sortieralgorithmen}

\subsection*{SelectionSort}
$\Theta(n^2)$, instable, in-place

\begin{algorithm}[H]
	\For{$i \gets 1$ \KwTo $n$}{
		$a \gets$ Minimum von $A$\;
		$B[i]=a$\;
		Lösche $a$ aus $A$\;
	}
	\KwRet $B$\;
\end{algorithm}

\subsection*{ExpoSort}
$\Theta(2^n)$

\begin{algorithm}[H]
	\If{$n>1$}{
		ExpoSort($A$, $n-1$)\;
		\If{$A[n-1] > A[n]$}{
			swap $A[n-1]$, $A[n]$\;
		}
		ExpoSort($A$, $n-1$)
	}
\end{algorithm}

\subsection*{InsertionSort}
$\bigO(n^2)$, stable, in-place

\begin{algorithm}[H]
	\For{$j\gets 2$ \KwTo $A$.len} {
		$k \gets A[j]$\;
		$i \gets j-1$\;
		\While{$i>0$ und $A[i]>k$}{
			$A[i+1] \gets A[i]$\;
			$i \gets i-1$\;
		}
		$A[i+1] \gets k$\;
	}
\end{algorithm}

\subsection*{BubbleSort}
$\bigO(n^2)$, stable, in-place

\begin{algorithm}[H]
	\For{$i \gets 1$ \KwTo $A.len - 1$}{
		\For{$j \gets A.len$ \textbf{downto} $i+1$}{
			\If{$A[j]<A[j-1]$}{
				swap $A[j]$, $A[j-1]$\;
			}
		}
	}
\end{algorithm}

\subsection*{MergeSort($A[1\ldots n]$)}
$\Theta(n\log n)$, stable, nicht in-place

\begin{algorithm}[H]
	\eIf{$n>1$}{
		$l \gets$ MergeSort($A[1\ldots \lfloor\frac{n}{2}\rfloor]$)\;
		$r \gets$ MergeSort($A[\lfloor\frac{n}{2}\rfloor+1 \ldots n]$)\;
		\KwRet merge($l$, $r$)\;
	}{
		\KwRet $A$\;
	}
\end{algorithm}

\subsubsection*{merge($X[1\ldots k]$, $Y[1\ldots l]$)}
\begin{algorithm}[H]
	\If{$k=0$}{
		\KwRet $Y[1\ldots l]$\;
	}
	\If{$l=0$}{
		\KwRet $X[1\ldots k]$\;
	}
	\eIf{$X[1] \le Y[1]$}{
		\KwRet $X[1] \circ \text{merge}(X[2\ldots k], Y[1\ldots l])$\;
	}{
		\KwRet $Y[1] \circ \text{merge}(X[1\ldots k], Y[2\ldots l])$\;
	}
\end{algorithm}

\subsection*{QuickSort}
$\Theta(n \log n)$, instable, in-place\\
(randomisiert worst case $\bigO(n^2)$)

\begin{algorithm}[H]
	\If{$|A| = 1$}{
		\KwRet $A$\;
	}
	Wähle $p\in A$ als Pivot\;
	$A_{<} \gets \{a\in A \mid a < p\}$\;
	$A_{=} \gets \{a\in A \mid a = p\}$\;
	$A_{>} \gets \{a\in A \mid a > p\}$\;
	\KwRet [QS($A_{<}$), $A_{=}$, QS($A_{>}$)]\;
\end{algorithm}

\subsection*{TODO: Counting-/RadixSort}
